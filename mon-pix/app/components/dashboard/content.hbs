<section class="page-container dashboard-content">
  <h1 class="sr-only">{{t 'pages.dashboard.title'}}</h1>

  {{#if (gt @model.length 0)}}
    <section class="dashboard-content__campaign-participation-overviews">
      <header>
        <h2 class="dashboard-content__title">{{t 'pages.dashboard.campaigns.title'}}</h2>
        <p class="dashboard-content__info">{{t 'pages.dashboard.campaigns.subtitle'}}</p>
      </header>

      <CampaignParticipationOverview::Grid @model={{@model}}/>
    </section>
  {{/if}}
</section>
