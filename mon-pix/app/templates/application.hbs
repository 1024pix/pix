{{title 'Pix' prepend=true}}
<HeadLayout />
<div class="body">
  <CommunicationBanner />
  {{outlet}}

  <!-- Preloading images -->
  <img src="{{rootURL}}/images/loader-white.svg" alt="Chargement en cours" style="display: none">
</div>
