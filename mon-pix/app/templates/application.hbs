{{page-title (t 'navigation.pix')}}
<HeadLayout />
<div class="body">
  <CommunicationBanner />
  {{outlet}}

  <!-- Preloading images -->
  <img src="{{rootURL}}/images/loader-white.svg" alt="{{t 'common.loading.default'}}" style="display: none">
</div>
