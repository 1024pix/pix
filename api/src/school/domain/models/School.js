class School {
  constructor({ id, name, code, organizationLearners } = {}) {
    this.id = id;
    this.name = name;
    this.code = code;
    this.organizationLearners = organizationLearners;
  }
}

export { School };
