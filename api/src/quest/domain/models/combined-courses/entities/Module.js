export class Module {
  constructor({ id, title, slug, duration, image, shortId }) {
    this.id = id;
    this.title = title;
    this.slug = slug;
    this.duration = duration;
    this.image = image;
    this.shortId = shortId;
  }
}
