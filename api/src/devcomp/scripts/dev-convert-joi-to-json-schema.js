// eslint-disable-next-line @eslint-community/eslint-comments/disable-enable-pair
/* eslint-disable no-console */

import { convertJoiToJsonSchema } from '../infrastructure/datasources/conversion/joi-to-json-schema.js';
import { moduleSchema } from '../infrastructure/datasources/learning-content/validation/module-schema.js';

const jsonSchema = JSON.stringify(convertJoiToJsonSchema(moduleSchema), null, 2);

console.log('JSON schema generated by convertJoiToJsonSchema :', jsonSchema);
