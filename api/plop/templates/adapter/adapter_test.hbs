const { expect, factory } = require('../../../test-helper');
const {{camelCase name}}Adapter = require('../../../../lib/infrastructure/adapters/{{kebabCase name}}-adapter');

describe('Unit | Infrastructure | Adapter | {{camelCase name}}Adapter', () => {

  beforeEach(() => {

  });

  afterEach(() => {

  });

  xit('should adapt {{name}} object to domain', () => {
    // given

    // when

    // then
    return expect(false).to.be.true;
  });
});
