/**
 * Traduction: Élement de correction portant sur la conformité d'une réponse
 * Context:    Objet existant dans le cadre de la correction d'une réponse pendant le fonctionnement
 *             interne de l'algorithme.
 */
class Validation {

  constructor({
    // attributes
    result,
    resultDetails,
    // includes
    // references
  } = {}) {
    // attributes
    this.result = result;
    this.resultDetails = resultDetails;
    // includes
    // references
  }
}

module.exports = Validation;
