class Validation {

  constructor({
    // attributes
    result,
    resultDetails,
    // includes
    // references
  } = {}) {
    // attributes
    this.result = result;
    this.resultDetails = resultDetails;
    // includes
    // references
  }
}

module.exports = Validation;
