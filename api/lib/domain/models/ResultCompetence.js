class ResultCompetence {
  constructor({ id, index, level, name, score } = {}) {
    this.id = id;
    this.index = index;
    this.level = level;
    this.name = name;
    this.score = score;
  }
}

export default ResultCompetence;
