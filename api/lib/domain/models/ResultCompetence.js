class ResultCompetence {
  constructor({
    id,
    // attributes
    index,
    level,
    name,
    score,
    // includes
    // references
  } = {}) {
    this.id = id;
    // attributes
    this.index = index;
    this.level = level;
    this.name = name;
    this.score = score;
    // includes
    // references
  }
}

module.exports = ResultCompetence;
