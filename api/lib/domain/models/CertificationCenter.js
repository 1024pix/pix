class CertificationCenter {

  constructor({
    id,
    // attributes
    name,
    createdAt,
    // includes
    // references
  } = {}) {
    this.id = id;
    // attributes
    this.name = name;
    this.createdAt = createdAt;
    // includes
    // references
  }
}

module.exports = CertificationCenter;
