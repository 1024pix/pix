class OrganizationRole {

  constructor({
    id,
    // attributes
    name
    // includes
    // references
  } = {}) {
    this.id = id;
    // attributes
    this.name = name;
    // includes
    // references
  }

}

module.exports = OrganizationRole;
