class Hint {

  constructor({
    // attributes
    skillName,
    value,
    // includes
    // references
  } = {}) {
    // attributes
    this.skillName = skillName;
    this.value = value;
    // includes
    // references
  }
}

module.exports = Hint;
