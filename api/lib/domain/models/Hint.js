class Hint {

  constructor({
    // attributes
    skillName,
    value,
    // embedded
    // relations
  } = {}) {
    // attributes
    this.skillName = skillName;
    this.value = value;
    // embedded
    // relations
  }
}

module.exports = Hint;
