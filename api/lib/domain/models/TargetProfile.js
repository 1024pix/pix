class TargetProfile {
  constructor({
    // attributes
    // embedded
    skills = []
    // relations
  } = {}) {
    // attributes
    // embedded
    this.skills = skills;
    // relations
  }
}

module.exports = TargetProfile;
