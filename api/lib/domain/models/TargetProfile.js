class TargetProfile {
  constructor({
    // attributes
    // includes
    skills = []
    // references
  } = {}) {
    // attributes
    // includes
    this.skills = skills;
    // references
  }
}

module.exports = TargetProfile;
