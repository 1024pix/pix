class CompetenceTree {

  constructor({
    id = 1,
    // attributes
    // embedded
    areas = [],
    // relations
  } = {}) {
    this.id = id;
    // attributes
    // embedded
    this.areas = areas;
    // relations
  }
}

module.exports = CompetenceTree;
