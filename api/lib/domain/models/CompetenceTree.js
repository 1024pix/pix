class CompetenceTree {

  constructor({
    id = 1,
    // attributes
    // includes
    areas = [],
    // references
  } = {}) {
    this.id = id;
    // attributes
    // includes
    this.areas = areas;
    // references
  }
}

module.exports = CompetenceTree;
