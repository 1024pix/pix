class PixRole {

  constructor({
    id,
    // attributes
    name,
    // embedded
    // relations
  } = {}) {
    this.id = id;
    // attributes
    this.name = name;
    // embedded
    // relations
  }
}

module.exports = PixRole;
