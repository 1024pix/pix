class BadgePartnerCompetence {

  constructor({
    id,
    name,
    skillIds,
  } = {}) {
    this.id = id;
    this.name = name;
    this.skillIds = skillIds;
  }
}

module.exports = BadgePartnerCompetence;
