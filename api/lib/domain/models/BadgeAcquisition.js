class BadgeAcquisition {
  constructor({
    id,
    // attributes
    // includes
    // references
    userId,
    badgeId,
  } = {}) {
    this.id = id;
    // attributes
    // includes
    // references
    this.userId = userId;
    this.badgeId = badgeId;
  }
}

module.exports = BadgeAcquisition;
