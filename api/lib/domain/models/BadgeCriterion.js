class BadgeCriterion {

  constructor({
    id,
    // attributes
    scope,
    threshold,
    // includes
    // references
  } = {}) {
    this.id = id;
    // attributes
    this.scope = scope;
    this.threshold = threshold;
    // includes
    // references
  }
}

module.exports = BadgeCriterion;
