class Tutorial {

  constructor({
    id,
    // attributes
    duration,
    format,
    link,
    source,
    title
    // includes
    // references
  } = {}) {
    this.id = id;
    // attributes
    this.duration = duration;
    this.format = format;
    this.link = link;
    this.source = source;
    this.title = title;
    // includes
    // references
  }
}

module.exports = Tutorial;
