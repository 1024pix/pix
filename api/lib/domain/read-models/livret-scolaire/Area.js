class Area {

  constructor({
    id,
    // attributes
    code,
    name,
    title,
  } = {}) {
    this.id = id;
    // attributes
    this.code = code;
    this.name = name;
    this.title = title;
  }
}

module.exports = Area;
