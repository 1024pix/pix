const cleaBadgeCreationHandler = {
  handle: function(/*event*/) {
    /*
        - Decide whether the completed assessment should create a badge
        - Create it
        If the reaction to the event if the same as an existing command use the command
        Otherwise perform tasks with dependencies (repositories, ...)
     */
  }
};

module.exports = {
  cleaBadgeCreationHandler,
};
