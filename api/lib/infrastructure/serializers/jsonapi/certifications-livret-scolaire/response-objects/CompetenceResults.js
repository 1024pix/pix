
class CompetenceResults {

  constructor({
    // attributes
    level,
    competenceId,
  } = {}) {
    // attributes
    this.level = level,
    // includes
    this.competenceId = competenceId;
  }

}

module.exports = CompetenceResults;
