server {
    server_name localhost;
    listen <%= ENV['PORT'] %>;
    charset utf-8;
    location / {
        proxy_pass http://localhost:<%= ENV['NODE_PORT'] %>;
        add_header  X-Pix  pixou  always;
    }
}
