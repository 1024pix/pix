{{page-title (t "pages.sessions.detail.page-title" sessionId=this.model.session.id) replace=true}}

<div class="session-details-page">
  <Sessions::SessionDetails @model={{this.model}} />
  {{outlet}}
</div>