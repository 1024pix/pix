{{routes/authenticated/sessions/session-finalizer
  session=model
  finalizeSession=(action 'finalizeSession')
  openModal=(action 'openModal')
  closeModal=(action 'closeModal')
  showConfirmModal=showConfirmModal
}}
