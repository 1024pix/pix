<Routes::Authenticated::Sessions::SessionFinalizer @session={{model}} @finalizeSession={{action "finalizeSession"}} @openModal={{action "openModal"}} @closeModal={{action "closeModal"}} @showConfirmModal={{showConfirmModal}} />
