apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: pix
type: Opaque
stringData:
  # These are development secrets - change in production!
  AUTH_SECRET: "change-me-in-production-this-is-for-dev-only-minimum-32-chars"
  # Add other secrets as needed
