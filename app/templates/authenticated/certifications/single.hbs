<header class="page-header">
  <h1 class="page-title">Certification</h1>
  <div class="page-actions">
    {{certification-select id=(readonly certificationId) select=(action "onLoadCertification")}}
  </div>
</header>
  {{#if certificationId}}
  {{#bs-nav type="tabs" as |nav|}}
    {{#nav.item}}{{#nav.link-to 'authenticated.certifications.single.info' certificationId}}Informations{{/nav.link-to}}{{/nav.item}}
    {{#nav.item}}{{#nav.link-to 'authenticated.certifications.single.details' certificationId}}Détails{{/nav.link-to}}{{/nav.item}}
  {{/bs-nav}}
  {{/if}}
<section class="page-section {{if certificationId 'with-navs'}}">
  {{#if loading}}
    <div class="single-loading">
      <i class='fa fa-spinner fa-spin fa-lg'></i>
      Chargement...
    </div>
  {{else}}
    {{outlet}}
  {{/if}}
</section>
