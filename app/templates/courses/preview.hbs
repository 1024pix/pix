<div id="course-preview" data-id="{{ model.id }}">
    <div class="container">

        <div class="title">
            Prévisualisation du test #{{ model.id }}
        </div>

        <div class="rounded-panel course-information">
            <h3 class="course-name">{{ model.name }}</h3>
            <p class="course-description">{{ model.description }}</p>
        </div>

        <a class=".button .button-primary simulate-button" href="/courses/{{ model.id }}/">Simuler le test</a>
    </div>
</div>
