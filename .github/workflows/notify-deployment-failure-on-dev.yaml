on:
  pull_request:
    types:
      - labeled
      - unlabeled
  check_suite:
    types:
      - completed

jobs:
  check_status:
    runs-on: ubuntu-latest
    steps:
      - env:
          DESCRIPTION: ${{ github.event.description }}
        run: |
          echo $DESCRIPTION
