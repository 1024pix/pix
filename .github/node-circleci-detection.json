{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "fileMatch": [".*"],
      "matchStrings": ["# renovate datasource=(?<datasource>.*?) depName=(?<depName>.*?)\\s*default: (?<currentValue>.*)\\s"]
    }
  ]
}
