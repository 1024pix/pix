{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "fileMatch": ["config.yml"],
      "matchStrings": [
        "# renovate datasource=(?<datasource>.*?) depName=(?<depName>.*?)\\s*default: (?<currentValue>.*)\\s"
      ]
    }
  ]
}
