config:
  phases:
    - duration: 3
      arrivalRate: 2
      name: "Test connectivity"
plugins:
  expect: {}
scenarios:

  - name: 'Obtenir la version de l API'
    flow:
      - get:
          url: "/api"
          expect:
            - statusCode: 200
