<div class="course-banner">

  <div class="container course-banner-wrapper">

    <span class="course-banner-name">{{course.name}}</span>

    {{#if withHomeLink}}
      {{#link-to "index" class="course-banner-home-link"}}Retour à la liste des tests{{/link-to}}
    {{/if}}

  </div>

</div>
