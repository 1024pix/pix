<div class="course-banner__container">

  <h1 class="course-banner__name">{{course.name}}</h1>

  {{#if withHomeLink}}
    {{#link-to "index" class="course-banner__home-link"}}Retour à la liste des tests{{/link-to}}
  {{/if}}

</div>
