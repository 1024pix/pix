{{page-title (t "pages.places.title")}}

<Places::Title />

<Places::CapacityAlert @occupied={{@model.occupied}} @total={{@model.total}} />
<Places::Statistics @model={{@model}} />
<Places::PlaceInfo @currentUser={{this.currentUser}} />