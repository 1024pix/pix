{{page-title (t "pages.students-sup-import.title")}}

<PixReturnTo @route="authenticated.sup-students.list">
  {{t "common.actions.back"}}
</PixReturnTo>

<Student::Sup::Import
  @onImportStudents={{this.importStudents}}
  @onReplaceStudents={{this.replaceStudents}}
  @isLoading={{this.isLoading}}
/>