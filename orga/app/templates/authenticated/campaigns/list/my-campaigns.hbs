{{page-title (t "pages.campaigns-list.tabs.my-campaigns")}}

{{#if @model.campaigns.meta.hasCampaigns}}
  <Campaign::List
    @caption={{t "pages.campaigns-list.table.description-my-campaigns"}}
    @organizationId={{@model.organizationId}}
    @campaigns={{@model.campaigns}}
    @nameFilter={{this.name}}
    @statusFilter={{this.status}}
    @ownerNameFilter={{this.ownerName}}
    @onFilter={{this.triggerFiltering}}
    @onClear={{this.clearFilters}}
    @onClickCampaign={{this.goToCampaignPage}}
    @canDelete={{true}}
    @showCampaignOwner={{true}}
    @onDeleteCampaigns={{this.onDeleteCampaigns}}
  />
{{else}}
  <Campaign::NoCampaignPanel />
{{/if}}