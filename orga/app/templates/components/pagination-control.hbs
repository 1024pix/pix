<div class="pagination-control content-text content-text--small">
  <div class="page-size">
    <div class="page-size__label">Voir</div>
    <div class="page-size__choice">
      <select class="content-text content-text--small" {{on "change" this.changePageSize}}>
        <option value="10" selected="{{if (eq this.pagination.pageSize 10) true}}">10</option>
        <option value="25" selected="{{if (eq this.pagination.pageSize 25) true}}">25</option>
        <option value="50" selected="{{if (eq this.pagination.pageSize 50) true}}">50</option>
      </select>
      <FaIcon class='page-size__choice--icon' @icon='sort'></FaIcon>
    </div>
  </div>
  <div class="page-navigation">
    <div class="page-navigation__arrow page-navigation__arrow--previous {{if (eq this.pagination.page 1) "page-navigation__arrow--disabled"}}">
      <LinkTo @route={{this.paginationLink}} @query={{hash pageNumber=this.previousPage}} @disabledWhen={{eq this.pagination.page 1}} @replace={{true}} class="icon-button icon-button--link">
        <FaIcon @icon='arrow-left'></FaIcon>
      </LinkTo>
    </div>
    <div>
      Page {{this.pagination.page}} / {{this.pagination.pageCount}}
    </div>
    <div class="page-navigation__arrow page-navigation__arrow--next {{if (eq this.pagination.page this.pagination.pageCount) "page-navigation__arrow--disabled"}}">
      <LinkTo @route={{this.paginationLink}} @query={{hash pageNumber=this.nextPage}} @disabledWhen={{eq this.pagination.page this.pagination.pageCount}} @replace={{true}} class="icon-button icon-button--link">
        <FaIcon @icon='arrow-right'></FaIcon>
      </LinkTo>
    </div>
  </div>
</div>
