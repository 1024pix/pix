{{#link-to "authenticated.campaigns.details" campaign.id
           class="panel panel--link campaign-list__item campaign-item"}}
  <div class="campaign-name">
    {{campaign.name}}
  </div>
  <div class="participations-counts">
    <div class="participations-counts__item participations-counts__item--border">
      <p class="label-text participations-counts__label">Participants</p>
      <p class="content-text content-text--big participations-counts__value">{{participationsCount}}</p>
    </div>
    <div class="participations-counts__item">
      <p class="label-text participations-counts__label">Profils reçus</p>
      <p class="content-text content-text--big participations-counts__value">{{sharedParticipationsCount}}</p>
    </div>
  </div>
{{/link-to}}
