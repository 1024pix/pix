<PixSelect
  @screenReaderOnly={{true}}
  @onChange={{this.onChange}}
  @options={{@options}}
  @value={{@selectedOption}}
  @placeholder={{@emptyOptionLabel}}
>
  <:label>{{@label}}</:label>
</PixSelect>