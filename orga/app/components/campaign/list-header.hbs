<header class="campaign-list-header__header hide-on-mobile">
  <h1 class="sr-only">{{@title}}</h1>
  <button
    role="tab"
    type="submit"
    class="campaign-list-header__tab {{unless @isArchived "campaign-list-header__tab--active"}}"
    aria-selected="{{not @isArchived}}"
    aria-label={{t "pages.campaigns-list.action.ongoing.aria-label"}}
    {{on "click" (fn @onClickStatusFilter null)}}
  >
    {{t "pages.campaigns-list.action.ongoing.label"}}
  </button>
  <button
    role="tab"
    type="submit"
    class="campaign-list-header__tab {{if @isArchived "campaign-list-header__tab--active"}}"
    aria-selected="{{@isArchived}}"
    aria-label={{t "pages.campaigns-list.action.archived.aria-label"}}
    {{on "click" (fn @onClickStatusFilter "archived")}}
  >
    {{t "pages.campaigns-list.action.archived.label"}}
  </button>
  <PixButtonLink @route="authenticated.campaigns.new" class="campaign-list-header__create-campaign-button">
    {{t "pages.campaigns-list.action.create"}}
  </PixButtonLink>
</header>