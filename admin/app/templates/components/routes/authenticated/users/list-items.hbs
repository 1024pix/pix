<div class="user-list">
  <div class="table content-text content-text--small">
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Prénom</th>
          <th>Nom</th>
          <th>Adresse électronique</th>
        </tr>
        <tr>
          <th></th>
          <th>{{input id="firstName" value=firstNameValue keyUp=(action triggerFiltering 'firstName' firstNameValue)}}</th>
          <th>{{input id="lastName" value=lastNameValue keyUp=(action triggerFiltering 'lastName' lastNameValue)}}</th>
          <th>{{input id="email" value=emailValue keyUp=(action triggerFiltering 'email' emailValue)}}</th>
        </tr>
      </thead>

      {{#if users}}
        <tbody>
        {{#each users as |user|}}
          <tr>
            <td>{{user.id}}</td>
            <td>{{user.firstName}}</td>
            <td>{{user.lastName}}</td>
            <td>{{user.email}}</td>
          </tr>
        {{/each}}
        </tbody>
      {{/if}}
    </table>

    {{#unless users}}
      <div class="table__empty content-text">Aucun résultat</div>
    {{/unless}}
  </div>
</div>

{{#if users}}
  {{pagination-control pagination=users.meta paginationLink="authenticated.users.list"}}
{{/if}}
