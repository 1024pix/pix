{{page-title "Orga " @model.organization.id " | Équipe"}}
{{#if this.accessControl.hasAccessToOrganizationActionsScope}}
  <Organizations::TeamActionsSection
    @addOrganizationMembership={{this.addOrganizationMembership}}
    @userEmailToAdd={{this.userEmailToAdd}}
    @onChangeUserEmailToAdd={{this.onChangeUserEmailToAdd}}
  />
{{/if}}

<Organizations::TeamSection
  @organizationMemberships={{@model.organizationMemberships}}
  @isLoading={{this.isLoading}}
  @firstName={{this.firstName}}
  @lastName={{this.lastName}}
  @email={{this.email}}
  @organizationRole={{this.organizationRole}}
  @triggerFiltering={{this.triggerFiltering}}
  @selectRoleForSearch={{this.selectRoleForSearch}}
/>