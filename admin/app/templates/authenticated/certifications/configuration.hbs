<section class="page-section">
  <form {{on "submit" this.onCompetenceScoringConfigurationSubmit}}>
    <PixTextarea
      @id="competence-configuration"
      @value={{this.competenceScoringConfiguration}}
      {{on "change" this.onCompetenceScoringConfigurationChange}}
    >
      <:label>Ajout de la configuration de niveau par compétence (JSON) :</:label>
    </PixTextarea>
    <PixButton @type="submit">Envoyer</PixButton>
  </form>

  <form {{on "submit" this.onCertificationScoringConfigurationSubmit}}>
    <PixTextarea
      @id="certification-scoring-configuration"
      @value={{this.certificationScoringConfiguration}}
      {{on "change" this.onCertificationScoringConfigurationChange}}
    >
      <:label>Ajout de la configuration de score de certification (JSON) :</:label>
    </PixTextarea>
    <PixButton @type="submit">Envoyer</PixButton>
  </form>
</section>