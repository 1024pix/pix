{{page-title "Profil " @model.targetProfile.id " Orgas | Pix4Pix Admin" replace=true}}

<TargetProfiles::Organizations
  @organizations={{@model.organizations}}
  @targetProfile={{@model.targetProfile}}
  @id={{this.id}}
  @name={{this.name}}
  @type={{this.type}}
  @externalId={{this.externalId}}
  @triggerFiltering={{this.triggerFiltering}}
  @goToOrganizationPage={{this.goToOrganizationPage}}
  @detachOrganizations={{this.detachOrganizations}}
/>