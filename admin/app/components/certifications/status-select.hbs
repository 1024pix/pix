<div class="certification-info-field certification-status-select">
  {{#if @edition}}
    <PixSelect
      @label="Statut :"
      class="certification-status-select__select"
      @options={{this.options}}
      @onChange={{this.selectOption}}
      @value={{@certification.status}}
      @hideDefaultOption={{true}}
    />
  {{else}}
    <span>Statut :</span>
    <span>{{@certification.statusLabelAndValue.label}}</span>
  {{/if}}
</div>