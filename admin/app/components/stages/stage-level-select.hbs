<PixSelect
  ...attributes
  @options={{this.levelOptions}}
  @onChange={{@onChange}}
  @value={{@value}}
  @id={{@id}}
  @isDisabled={{@isDisabled}}
  @screenReaderOnly={{true}}
  @hideDefaultOption={{true}}
>
  <:label>{{@label}}</:label>
</PixSelect>