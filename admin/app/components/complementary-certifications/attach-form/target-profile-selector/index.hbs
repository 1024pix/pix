{{#if this.selectedTargetProfile}}
  <ComplementaryCertifications::AttachForm::TargetProfileSelector::SelectedTargetProfile
    @onChange={{this.onChange}}
    @attachableTargetProfile={{this.selectedTargetProfile}}
  />
{{else}}
  <ComplementaryCertifications::AttachForm::TargetProfileSelector::Searchbar
    @onSelection={{this.onSelection}}
    @onSearch={{this.onSearch}}
    @options={{this.attachableTargetProfiles}}
    @isLoading={{this.isAttachableTargetProfilesLoading}}
  />
{{/if}}