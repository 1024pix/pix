{{! template-lint-disable no-invalid-interactive }}
<th class="th--clickable" rowspan={{@thematic.tubes.length}} {{on "click" this.toggle}}>
  <label>
    <TargetProfiles::NewTubeBased::Checkbox
      id="thematic-{{@thematic.id}}"
      @state={{this.state}}
      {{on "change" this.onChange}}
    />
    {{@thematic.name}}
  </label>
</th>